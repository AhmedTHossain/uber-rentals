export const dynamic = "force-dynamic";

export default function TestPage() {
  return <div style={{ padding: 40, fontFamily: "sans-serif" }}>Test page — no DB, no auth. If you see this, the rendering pipeline works.</div>;
}
